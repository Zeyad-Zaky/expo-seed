import React from 'react';
import {View, Text} from 'react-native';

const ScreenScreen = () => {
    return (
        <View style={{flex:1, justifyContent: 'center', alignItems: 'center'}}>
            <Text>This is main screen tabs</Text>
        </View>
    );
};

export default ScreenScreen;
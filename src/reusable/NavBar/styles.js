import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  navbar: {
    maxHeight: '10%',
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
  },
  navItem: {
    width: '100%',
    maxWidth: '100%',
    maxHeight: '100%',
    marginRight: '20%',
    resizeMode: 'contain',  
  }
});